{
  "name": "aucklandsbestpunters",
  "version": "1.0.0",
  "private": true,
  "description": "Auckland's Best Punters — the club ledger",
  "scripts": {
    "build": "node -e \"console.log('No build step — public/ ships as is.')\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "logs": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
